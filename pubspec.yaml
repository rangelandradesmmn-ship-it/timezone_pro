name: timezone_pro
description: A modern, elegant world clock app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  intl: ^0.19.0
  timezone: ^0.9.2
  sqflite: ^2.3.0
  path: ^1.9.0
  shared_preferences: ^2.2.3
  flag: ^0.3.3
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
